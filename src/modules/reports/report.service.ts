// Reporting/analytics domain (Part C). The dashboard + analytics queries live in
// src/lib/queries/*; re-exported here for the team's `@/modules/reports` convention.
export { getAnalyticsData } from "@/lib/queries/analytics";
export { getDashboardData } from "@/lib/queries/dashboard";
